The code will use the database with the file "compressed_database.bin".
As a temporary measure to check the difference between the 2012 and 2014 models,
just copy the appropriate file with the name "compressed_database.bin".
